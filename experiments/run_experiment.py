"""Automated experiment execution.

NOT YET IMPLEMENTED — scaffold only. Implemented by T8.2.

Config-driven runs: run_id generation, per-trial seed derivation, receiver and
sender orchestration, abort timeout, and capture of the full frozen config into
summary.json (RP-01, RP-02).
"""
