[pytest]
# Collect only from test/. Without this, a bare `pytest` from the project root
# also matches smoke_udp.py by filename pattern and imports it — that file is a
# runnable script, not a test module, and importing it is not what anyone means
# by running the test suite. The suite is test/ and nothing else.
testpaths = test
python_files = test_*.py
